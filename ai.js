let humanFile = require('./human');


class Ai extends Player {
    constuctor(name, array, rules, players, results)
    super(name)
}



// random function 
function randomSelection(){
    
    let random = Math.random() * gameArray.length
    random = Math.floor(random);
    console.log(gameArray[random]) 
}